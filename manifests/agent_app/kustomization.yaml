---
configMapGenerator:
  - name: agent-config
    envs:
      - ./application.properties

secretGenerator:
  - name: agent-secret
    envs:
      - ./.env

resources:
  - rbac.yaml
  - deploy.yaml
  - service.yaml
