---
configMapGenerator:
  - name: bucket-config
    envs:
      - ./application.properties

secretGenerator:
  - name: bucket-secret
    envs:
      - ./.env

resources:
  - deploy.yaml
  - service.yaml
